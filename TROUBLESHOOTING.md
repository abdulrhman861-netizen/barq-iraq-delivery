# استكشاف المشاكل الشائعة

## 1) التطبيق لا يبدأ
- نفّذ `npm install` ثم `npm start`.
- امسح الكاش: `expo start --clear`.

## 2) مشاكل Firebase
- تأكد من وجود `.env.local`.
- تحقق من جميع مفاتيح `FIREBASE_*`.
- تأكد أن Realtime Database مفعّل.

## 3) OTP لا يصل
- جرّب رقم اختبار Firebase Auth.
- تحقق من إعداد Phone Auth في Firebase Console.

## 4) الخريطة لا تظهر
- تحقق من `GOOGLE_MAPS_API_KEY`.
- تأكد من صلاحية API Key للمشروع الصحيح.

## 5) مشاكل الدفع التجريبي
- استخدم مفاتيح اختبار فقط.
- لا تحفظ أي أسرار حقيقية داخل المستودع.

## 6) الأداء ضعيف
- أغلق التطبيقات الخلفية على المحاكي/الجهاز.
- راقب السجلات عبر `npm run logs`.
